diff --git a/src/content-handlers/iiif/modules/uv-openseadragoncenterpanel-module/css/styles.less b/src/content-handlers/iiif/modules/uv-openseadragoncenterpanel-module/css/styles.less
index b2622a8..1a960e8 100644
--- a/src/content-handlers/iiif/modules/uv-openseadragoncenterpanel-module/css/styles.less
+++ b/src/content-handlers/iiif/modules/uv-openseadragoncenterpanel-module/css/styles.less
@@ -233,6 +233,8 @@
 
       .lineAnnotationRect {
         .opacity(0.5);
+        border: 2px solid rgba(120, 251, 185, 0.7);
+        box-sizing: border-box;
         &.current {
           @col: rgb(120, 251, 185);
           background-color: saturate(@col, 100%);
diff --git a/src/content-handlers/iiif/modules/uv-textrightpanel-module/TextRightPanel.ts b/src/content-handlers/iiif/modules/uv-textrightpanel-module/TextRightPanel.ts
index e9d30db..20499fb 100644
--- a/src/content-handlers/iiif/modules/uv-textrightpanel-module/TextRightPanel.ts
+++ b/src/content-handlers/iiif/modules/uv-textrightpanel-module/TextRightPanel.ts
@@ -352,15 +352,34 @@ export class TextRightPanel extends RightPanel<TextRightPanelConfig> {
       const altoDoc = new DOMParser().parseFromString(data, "application/xml");
       const textLines = altoDoc.querySelectorAll("TextLine");
 
+      // ALTO coords are in the processed image's pixel space (its Page
+      // WIDTH/HEIGHT), which may be smaller than the displayed image when
+      // the HTR pipeline width-capped its input. Scale into displayed-image
+      // pixels before converting to viewport coords.
+      const altoPage = altoDoc.querySelector("Page");
+      const altoPageWidth = Number(altoPage && altoPage.getAttribute("WIDTH"));
+      const altoPageHeight = Number(altoPage && altoPage.getAttribute("HEIGHT"));
+      const resList: any = this.extension.resources;
+      const displayed: any =
+        resList && this.index >= 0 ? resList[this.index] : null;
+      const scaleX =
+        displayed && displayed.width && altoPageWidth
+          ? displayed.width / altoPageWidth
+          : 1;
+      const scaleY =
+        displayed && displayed.height && altoPageHeight
+          ? displayed.height / altoPageHeight
+          : 1;
+
       const lines = Array.from(textLines).map((e, i) => {
         const strings = e.querySelectorAll("String");
         const t = Array.from(strings).map((e, i) => {
           return e.getAttribute("CONTENT");
         });
-        let x = Number(e.getAttribute("HPOS"));
-        const y = Number(e.getAttribute("VPOS"));
-        const width = Number(e.getAttribute("WIDTH"));
-        const height = Number(e.getAttribute("HEIGHT"));
+        let x = Number(e.getAttribute("HPOS")) * scaleX;
+        const y = Number(e.getAttribute("VPOS")) * scaleY;
+        const width = Number(e.getAttribute("WIDTH")) * scaleX;
+        const height = Number(e.getAttribute("HEIGHT")) * scaleY;
         x =
           x +
           this.offsetX +
@@ -416,10 +435,13 @@ export class TextRightPanel extends RightPanel<TextRightPanelConfig> {
             this.setCurrentLineAnnotationRect(e.target);
           });
           // Add overlay to OpenSeadragon canvas
-          const osRect = new OpenSeadragon.Rect(x, y, width, height);
-          (<OpenSeadragonExtension>(
-            this.extension
-          )).centerPanel.viewer.addOverlay(div[0], osRect);
+          // ALTO coords are image pixels; convert to OSD viewport coords
+          const osdViewer = (<OpenSeadragonExtension>this.extension)
+            .centerPanel.viewer;
+          const osRect = osdViewer.viewport.imageToViewportRectangle(
+            new OpenSeadragon.Rect(x, y, width, height)
+          );
+          osdViewer.addOverlay(div[0], osRect);
 
           line.on("keydown", (e: any) => {
             if (e.keyCode === 13) {
diff --git a/src/uv.html b/src/uv.html
index e19584d..942da9a 100644
--- a/src/uv.html
+++ b/src/uv.html
@@ -34,18 +34,18 @@
         uv = UV.init("uv", data);
 
 
-        const configUrl = urlAdapter.get('config');
-        if (configUrl) {
-          uv.on("configure", function({ config, cb }) {
-            cb(
-              // new Promise(function (resolve) {
-              //   fetch(configUrl).then(function (response) {
-              //     resolve(response.json());
-              //   });
-              // })
-            );
-          })
-        }
+        // uv-iiif-config.json carries options (e.g. textRightPanelEnabled) that
+        // are not compiled into UV.js and must be supplied via the configure event
+        const configUrl = urlAdapter.get('config') || 'uv-iiif-config.json';
+        uv.on("configure", function({ config, cb }) {
+          cb(
+            new Promise(function (resolve) {
+              fetch(configUrl).then(function (response) {
+                resolve(response.json());
+              });
+            })
+          );
+        })
 
         var $UV = document.getElementById("uv");
 
--- a/src/content-handlers/iiif/modules/uv-openseadragoncenterpanel-module/OpenSeadragonCenterPanel.ts
+++ b/src/content-handlers/iiif/modules/uv-openseadragoncenterpanel-module/OpenSeadragonCenterPanel.ts
@@ -1140,27 +1140,31 @@
   }
 
   clearAnnotations(): void {
-    // Due to a bug(?) in OpenSeadragon, we're it moves all line annotations as children directly under the body node in the DOM,
-    // we need to readd our line annotation rects after a clear.
+    // OpenSeadragon's clearOverlays() destroys our line-annotation overlays,
+    // and Overlay.destroy() re-appends each element to document.body with
+    // display:none. Re-add the CURRENT canvas's rects afterwards. Two fixes
+    // over the original: the selector must match body-orphaned rects (the
+    // old "div > div.lineAnnotationRect" cannot), and the data-* pixel rect
+    // must be converted to viewport coords (same fix as TextRightPanel).
     this.viewer.clearOverlays();
-    let parentNodeName = "";
-    const lineAnnotationRects = $("div > div.lineAnnotationRect");
-    if (lineAnnotationRects[0]) {
-      parentNodeName = lineAnnotationRects[0].parentNode.nodeName.toLowerCase();
-      if (parentNodeName === "body") {
-        $(lineAnnotationRects).each((i: number, e: any) => {
-          const x = Number(e.getAttribute("data-x"));
-          const y = Number(e.getAttribute("data-y"));
-          const width = Number(e.getAttribute("data-width"));
-          const height = Number(e.getAttribute("data-height"));
-          const osRect = new OpenSeadragon.Rect(x, y, width, height);
-          (<OpenSeadragonExtension>(
-            this.extension
-          )).centerPanel.viewer.addOverlay(e, osRect);
-          e.style.display = "block";
-        });
+    const currentCanvasIndex = this.extension.helper.canvasIndex;
+    $("div.lineAnnotationRect").each((i: number, e: any) => {
+      if (!e.parentNode || e.parentNode.nodeName.toLowerCase() !== "body") {
+        return;
       }
-    }
+      if (Number(e.id.split("-")[2]) !== currentCanvasIndex) {
+        return;
+      }
+      const x = Number(e.getAttribute("data-x"));
+      const y = Number(e.getAttribute("data-y"));
+      const width = Number(e.getAttribute("data-width"));
+      const height = Number(e.getAttribute("data-height"));
+      const osRect = this.viewer.viewport.imageToViewportRectangle(
+        new OpenSeadragon.Rect(x, y, width, height)
+      );
+      this.viewer.addOverlay(e, osRect);
+      e.style.display = "block";
+    });
   }
 
   getAnnotationsForCurrentImages(): AnnotationGroup[] {
