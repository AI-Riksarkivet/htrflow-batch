# Archived decks

The first two decks, superseded by the five-part series one level up. They
are kept for their speaker notes and are not published: `scripts/slides.sh`
builds only the decks at the top of `docs/slides`. Their diagrams are the
`gitops-and-kueued-htrflow-*` and `htrflow-batch-under-the-hood-*` files in
`../assets`; to build one again, move it back up.
