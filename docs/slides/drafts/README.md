# Draft decks

Parts 2 to 5, held back while part 1 is finished. They are not published:
`scripts/slides.sh` builds only the decks at the top of `docs/slides`. Their
diagrams are still generated into `../assets` by `../diagrams/build_rest.py`.
To publish one, move it back up and add it to `docs/presentations.md`.
