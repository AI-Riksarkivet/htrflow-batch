---
type: Product Backlog Item
id: 2891
parent: 2800
title: Operations runbook index
---

# B57 · Operations runbook index

**Story.** As an operator, I want a page documenting one page that maps each failure class (stale status, needs-attention volume, warm-up stuck, HCP write errors, policy rejection, promotion failed) to the page and the command that resolves it, so that the procedure exists in writing before anyone needs it under pressure.

## Why it matters

Failure Handling explains the mechanism; on-call needs the shortest path from alert to action.

## What this delivers

- A new page under the relevant track covering one page that maps each failure class (stale status, needs-attention volume, warm-up stuck, HCP write errors, policy rejection, promotion failed) to the page and the command that resolves it, linked from the nav and from the stories that need it.
- Commands shown are the real ones, tested once by following the page.

## Done when

- [ ] Page exists and is linked from the nav; a colleague follows it end to end without asking a question.
