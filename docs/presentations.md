# Presentations

For people who know htrflow and want to see what runs it at archive scale.
A deck opens in the browser as a presentation (arrow keys move, `F` goes
full screen) or downloads as a PDF.

| Deck | What it covers | Open |
|---|---|---|
| **Distributed htrflow** | From one machine to many nodes, a rough architecture, a pod per archival volume, a Job per campaign, the window and the queue, priority, the bucket, git as the interface, the status page and the viewer | [slides](slides/part-1-from-one-folder-to-the-archive.html) · [PDF](slides/part-1-from-one-folder-to-the-archive.pdf) |

The deck is built from Markdown in
the repository's `docs/slides` with Marp, on every change to the main
branch.
