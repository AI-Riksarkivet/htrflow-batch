apiVersion: v2
name: htrflow-batch
description: Kueue-gated batch HTR platform around htrflow (queues, model cache and one web front serving the campaign browser, Universal Viewer and the status API). Campaigns are rendered as Indexed Jobs by packages/converter and applied by htrflow-campaigns apply, by hand or as an Argo CD hook, not by this chart.
type: application
# Bump on every template/values change (README "Changelog"); a stable
# version hid 21 revisions of drift on the PoC (audit O6).
version: 0.13.0
appVersion: "0.5.0"
annotations:
  artifacthub.io/license: EUPL-1.2
