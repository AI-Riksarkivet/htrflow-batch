htrflow-batch {{ .Chart.Version }} installed.
- LocalQueue: {{ .Values.queue.name }} (namespace {{ .Release.Namespace }}); ClusterQueue admits this namespace only.
- Kueue CRDs are a prerequisite, not installed by this chart.
- Model cache PVC: {{ .Values.modelCache.name }}{{ if .Values.modelCache.create }} (rendered, kept on uninstall){{ else }} (must already exist){{ end }}.
- Campaigns are Indexed Jobs rendered by `htrflow-campaigns render` and applied outside this chart
  (kubectl / Argo) — not by this chart. See docs/how-it-works/campaigns.md.
- Jobs run offline on a read-only model cache: warm each pipeline once (the converter renders
  `htr-warmup-<id>` alongside the pipeline ConfigMap).
- Pod Security: the chart does not own the namespace — label it once (`make psa-labels`, reads
  security.psaEnforce={{ .Values.security.psaEnforce }}; warn/audit=restricted).
- Upgrades: use --reset-then-reuse-values (plain --reuse-values keeps the old chart's defaults).
- If Jobs stay `queued` while the GPU is idle, check the Kueue controller before the GPU (O17).
{{- if .Values.network.enabled }}
- NetworkPolicies rendered{{ if .Values.network.defaultDeny }} with a namespace default deny{{ end }} (values: network.*).
  The S3 Secret ({{ .Values.s3.existingSecret }}) needs a `credentials` key in AWS ini format (docs: getting-started/deploy).
{{- end }}
{{- if .Values.viewer.enabled }}
- Viewer: NodePort {{ .Values.viewer.nodePort }}; reads campaigns from the read API at /api/v1/jobs.
{{- end }}
- Read API: Deployment htrflow-web, Service htrflow-web:8081 (in-cluster only, proxied by the viewer at /api/).
{{- if .Values.security.allowTagImages }}
- WARNING security.allowTagImages=true: control-plane images are tag-pinned and re-pulled on every rollout. PoC only.
{{- end }}
- PoC-only support infrastructure (RustFS, registry, nvidia device plugin) is a separate
  chart: charts/htrflow-devstack (`make install-devstack`).
